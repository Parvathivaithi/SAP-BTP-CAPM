_schema-version: 3.3.0
ID: DealerDetail
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: DealerDetail-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: DealerDetail-auth
      - name: DealerDetail-db
      - name: DealerDetail-destination

  - name: DealerDetail-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: DealerDetail-db

  - name: DealerDetail-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: DealerDetaildealer
          artifacts:
            - dealer.zip
          target-path: app/
    requires:
      - name: srv-api
      - name: DealerDetail-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true

  - name: DealerDetaildealer
    type: html5
    path: app/dealer
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: DealerDetail-destinations
    type: com.sap.application.content
    requires:
      - name: DealerDetail-html5-repo-host
        parameters:
          service-key:
            name: DealerDetail-html5-repo-host-key
      - name: srv-api
      - name: DealerDetail-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: DealerDetail-html5-repository
              ServiceInstanceName: DealerDetail-html5-repo-host
              ServiceKeyName: DealerDetail-html5-repo-host-key
              sap.cloud.service: DealerDetail.service

resources:
  - name: DealerDetail-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: DealerDetail-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
  - name: DealerDetail-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: DealerDetail-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true

    requires:
      - name: srv-api

  - name: DealerDetail-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
